# RTV Apparel Environment Variables
# Copy this file to storefront/.env and fill in your values
# NEVER commit the actual .env file!

# ===========================================
# SHOPIFY STOREFRONT API (Required)
# ===========================================
# Get these from Shopify Admin > Apps > Develop apps > Your app > API credentials

# Your Shopify store domain (e.g., raize-the-vibe.myshopify.com)
PUBLIC_STORE_DOMAIN=your-store.myshopify.com

# Storefront API access token (public, for client-side requests)
# This is the Storefront API access token from your custom app
PUBLIC_STOREFRONT_API_TOKEN=your-storefront-api-token

# Private Storefront API token (for server-side requests)
# This is the Admin API access token (starts with shpat_)
PRIVATE_STOREFRONT_API_TOKEN=your-private-storefront-token

# ===========================================
# SESSION (Required)
# ===========================================
# Generate with: openssl rand -hex 32
SESSION_SECRET=your-random-session-secret-here

# ===========================================
# STOREFRONT ID (Optional)
# ===========================================
# Used for analytics and multipass. Can be left empty for basic setup.
PUBLIC_STOREFRONT_ID=

# ===========================================
# CUSTOMER ACCOUNT API (Optional)
# ===========================================
# Only needed if implementing customer accounts login/registration
# Get from Shopify Admin > Settings > Customer accounts > Customer Account API
PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID=
PUBLIC_CUSTOMER_ACCOUNT_API_URL=
SHOP_ID=

# ===========================================
# CHECKOUT (Optional)
# ===========================================
# Custom checkout domain if using Shopify Plus
PUBLIC_CHECKOUT_DOMAIN=
